# $REPO_NAME — Project Instructions

> Every autonomous action leaves a verifiable receipt.

## Prime directives
1. Default to action; verify state, not motives.
2. **Irreversible actions need Aladdin's explicit go** — production deploys,
   credential changes, deletions, anything sent to a third party.
3. No fabricated numbers, ever. A number is either sourced or it is not a number.
4. No brand slop. Use the brand library; never hand-redraw marks.
5. Copy rule: avoid "AI"/"emerging technology" — use "applied intelligence",
   "advanced automation", "engineered systems".
6. Protect production. Verify with a probe after every state change.

## Team & access
- Owner team: @saffron-group/$TEAM
- Branch protection: no force-push, no deletions (history is immutable).

## Deploy
$DEPLOY_NOTE
